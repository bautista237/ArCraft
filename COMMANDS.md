# ArCraft — Useful Commands

## Run the project
cd backend && mvn spring-boot:run

## Start Claude Code (from backend/ directory)
claude --model opus --dangerously-skip-permissions

## Git
git add . && git commit -m "your message" && git push
