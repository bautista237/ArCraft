spring.datasource.url=jdbc:h2:file:/Users/juanbautistatassello/ArCraft/arcraft-data;AUTO_SERVER=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

spring.thymeleaf.cache=false

spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=false

# ── "Login with Minecraft" (Microsoft OAuth2) ──────────────────────────────
# Optional. Leave commented to keep plain username/password login only (cracked-friendly).
# To enable: register an Azure AD app (Personal Microsoft accounts), add redirect URI
#   http://<your-host>:8080/login/oauth2/code/microsoft
# and enable "Live SDK support". Then set the three values below (env vars recommended).
#
# spring.security.oauth2.client.registration.microsoft.client-id=${MS_CLIENT_ID}
# spring.security.oauth2.client.registration.microsoft.client-secret=${MS_CLIENT_SECRET}
# spring.security.oauth2.client.registration.microsoft.client-name=Minecraft
# spring.security.oauth2.client.registration.microsoft.provider=microsoft
# spring.security.oauth2.client.registration.microsoft.authorization-grant-type=authorization_code
# spring.security.oauth2.client.registration.microsoft.redirect-uri={baseUrl}/login/oauth2/code/microsoft
# spring.security.oauth2.client.registration.microsoft.scope=XboxLive.signin,offline_access
# spring.security.oauth2.client.provider.microsoft.authorization-uri=https://login.microsoftonline.com/consumers/oauth2/v2.0/authorize
# spring.security.oauth2.client.provider.microsoft.token-uri=https://login.microsoftonline.com/consumers/oauth2/v2.0/token
# spring.security.oauth2.client.provider.microsoft.user-name-attribute=username

# ── Event reminder emails (optional) ───────────────────────────────────────
# Disabled by default. Requires player email addresses and an SMTP server.
# arcraft.mail.enabled=true
# spring.mail.host=smtp.gmail.com
# spring.mail.port=587
# spring.mail.username=${SMTP_USER}
# spring.mail.password=${SMTP_PASS}
# spring.mail.properties.mail.smtp.auth=true
# spring.mail.properties.mail.smtp.starttls.enable=true
